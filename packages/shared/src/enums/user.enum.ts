export enum UserSsoProvider {
  GOOGLE = 'google',
  MICROSOFT = 'microsoft',
  LOCAL = 'local',
}

export enum AuditActorType {
  USER = 'user',
  SYSTEM = 'system',
  AI = 'ai',
  WEBHOOK = 'webhook',
}
